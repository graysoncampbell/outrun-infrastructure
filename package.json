{
  "name": "outrun-infrastructure",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "destroy": "read Environment && op inject -i variables.tf.tpl -o variables.tf && terraform destroy",
    "apply": "read Environment && op inject -i variables.tf.tpl -o variables.tf && terraform apply",
    "plan": "read Environment && op inject -i variables.tf.tpl -o variables.tf && terraform plan"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}