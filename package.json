{
  "name": "outrun-infrastructure",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "setup": "op inject -i variables.tf.tpl -o variables.tf && terraform init $$",
    "destroy": "terraform destroy",
    "apply": "terraform apply",
    "plan": "terraform plan"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mongodb": "^6.2.0"
  }
}
